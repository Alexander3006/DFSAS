'use strict';

const TransportType = {
  WebSocket: 'WS',
  HTTP: 'HTTP',
};

const EndpointMethods = {
  WS: 'WS',
  GET: 'GET',
  POST: 'POST',
};

module.exports = {
  TransportType,
  EndpointMethods,
};
